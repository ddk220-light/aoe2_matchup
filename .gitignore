# Python
venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Game data file (too large, users should copy from their own installation)
empires2_x2_p1.dat

# Extracted data (regenerated from dat file via python3 -m extraction.run)
extraction/extracted_data/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.venv
env/

# Temporary files
*.tmp
*.temp
ANALYSIS.md

# User data
webapp/matchup_votes.jsonl

# Battle score cache and DB (machine-local build artifacts)
webapp/battle_cache.json
webapp/battle_scores.db

# Downloaded wiki assets (fetched via download scripts, not source)
webapp/static/img/units-wiki/
webapp/static/img/units-wiki-gifs/

# One-off download/scraping scripts
webapp/download_wiki_*.py

# Scratch design notes
civ_detail_mockup.md
new-civ-view.md

# Claude Code worktree state
.claude/worktrees/
webapp/backups/

# Raw extracted game graphics (19GB local dump; never commit)
graphics/game_raw_files/

# (2026-06-11: graphics/art + sprite upscales are TRACKED again by owner decision —
# repo doubles as the backup for the generated art)
# Scratch / working files
.scratch/
tmp/
.tmp_research/

# Archived-but-kept files (see .old/README.md); local only
.old/

# Claude Code local state
.claude/launch.json
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Patch-pipeline local artifacts (pre-patch archives + intermediates;
# created by webapp/patch_pipeline.py, never committed)
extraction/extracted_data_prev/
webapp/aoe2_reference_*.db
webapp/changed_units_*.json
webapp/*.db.bak

# Local matchup working DB (in-repo Armenians-only stub removed 2026-06-11;
# real baselines live outside the repo, e.g. D:/AI/matchup_baseline_<build>.db)
webapp/matchup_db.db
aoe2x/batch/matchup_db.db
