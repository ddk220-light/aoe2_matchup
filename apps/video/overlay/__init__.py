"""overlay — card rendering, HUD drawing, footage OCR and ffmpeg compose.

Importable as a package (`from overlay.compose import ...`) whenever
`scenario_builder/` is on sys.path — which it is for every `python -m auto.*`
and `python -m overlay.*` entry point run from scenario_builder/.
"""
