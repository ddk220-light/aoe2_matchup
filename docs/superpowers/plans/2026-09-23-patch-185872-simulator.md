# Patch 185872 simulator integration

> Execute using the executing-plans and test-driven-development workflows.

**Goal:** Run the new roster in the existing JavaScript battle engine using the exact recorded setup, and report live-versus-simulation differences without outcome tuning.

**Architecture:** Reuse the existing DAT/reference exporter, versioned mechanics profiles, golden formations and `runFight`. Implement the setup contract documented by recorder commit `2cced342`, not a replacement engine. Generated candidates and comparisons remain local.

**Spec:** Owner's September 23 request: use E-drive recordings, at most 27 main units, the recorded cost formula/positions/dynamic buffer; integrate stats and abilities; compare five varied opponents for the new units, then remaining available matchups; report mismatches without fixing them. Rankings come later.

## Constraints

- No website, ranking, production database, asset, push or deployment changes.
- E-drive recordings are read-only. No new game captures.
- Purchase cost is per physical unit after maximum applicable discounts; weights F/W/G = 1/0.9/1.1.
- Main count: cheaper side 27; other side `max(1, floor(27*sqrt(cheap/dear)+0.5))`.
- Mixed fights only: P4 `floor(clamp(5+(rangedCount*weightedCost-1000)/1800,5,10)+0.5)`; first N golden slots, tenth `(7.5,5.5)`.
- No copied continuous trajectories, winner overrides or matchup-specific calibration.
- Missing ability semantics must be explicitly reported, never silently approximated as verified mechanics.
- Proportionate targeted checks, not unrelated hardening or broad repository test suites.

## Tasks

- [ ] 1. Setup contract: add `src/battle-setup.js::deriveRecordedBattleSetup(side2,side3,{cap=27})`, accepting class and already-discounted `effectiveCost`; return n2/n3, cost2/cost3, P4 count/owner and policy IDs. Extend `loadLabScenario` with optional P4 count, preserving historic defaults. RED/GREEN literal tests for 26/27/6, RvR no P4, and tenth slot.
- [ ] 2. Runtime profiles: extend existing exporter only for independently sourced new-unit effects and selected candidate extraction context. Reuse `build_profiles_for_row` and `runFight(mechanicsBySide)` instead of duplicate registry architecture. Test new charges/conditional behavior against source-derived expectations before implementation; flag unresolved rules.
- [ ] 3. Recorded comparison: prepare jobs from latest candidate reference and archive plans, verify generated counts/buffers/initial positions against frames, run Composite Bowman/Jaguar Warrior/Cataphract/Houfnice/Mangudai for each new unique unit, then the remaining corpus if supported. Save per-run winner, HP, survivors, duration and explicit setup/mechanics caveats. Do not tune differences.
- [ ] 4. Verify and independently review the scoped diff. Save a reproducible report with exact source paths, commit and mechanics provenance, limitations and next steps. Local-only completion.

## Review focus

- Captured cost/HP discrepancies remain visible instead of silently changing inputs to force agreement.
- Jarl's ranged-melee damage and scenario orientation must not be treated as ordinary melee or pierce arrows.
- Jomsviking torch cannot target ordinary land soldiers; Guard capacity-two charge cannot be treated as one full-capacity volley without evidence.
- P4 uses its own Spanish full-upgrade profile, not a new civilization's bonuses.
- Missing source definitions and unsupported opponents must not be reported as passing comparisons.

## Progress

### Staging continuation explicitly approved

- Owner supplied Shield Wall's +1/+1 per 15 OTHER infantry (max +3/+3), and
  Gothikon's two separately replenished axes, same melee damage, range 3,
  30 seconds per charge. Finish these mechanics and expose the updated roster
  in the existing public simulator, then publish to staging only.
- Use the established cost formula as the default, retain manual counts, and
  use the approved shared images/animations. No production or rankings update.
- Reuse the existing isolated worktree and this progress ledger. Verify the
  staged reference database, public battle-config/worker path, sprites and
  staging deployment; obtain independent review before the explicit staging push.
- Gothikon inter-throw timing is not given by the supplied wiki excerpt. Reuse
  the sourced normal weapon reload and special animation cycle; report this
  as a model assumption pending live timing validation, not a measured delay.

### September 23 continuation approved by owner

- Implement the missing combat technologies; preserve reusable mechanics rather than fitting winners.
- Select decisive existing recordings using winner remaining HP / winning army starting HP >= 60%. Use 50–60% only if required for coverage, explicitly labeled. Close fights are diagnostic only, not calibration pass/fail targets.
- First establish numeric technology semantics from game data and recorded per-hit behavior, then add failing behavior tests, implement, and run a bounded diverse comparison set.
- Continue in the existing isolated feature worktree. No new captures, rankings, website changes, commits/pushes or production writes requested.
- Existing setup and initial comparison work is retained; no rerun of unchanged artifact generation.
- Completed: Hamask own-HP attack, researched blast fields/Vendel fixed damage,
  carrier-inherited smart-mode8 charge damage, and >=60% calibration selection.
- Online primary sources plus installed Effects.xs resolve those rules. Shield
  Wall's integer interpolation and Gothikon charge consumption remain incomplete;
  the owner's follow-up requested online research rather than a game probe.
- Ten supported decisive cases completed (five Jarl/five Jomsviking): ten matching
  winners, zero errors, but HP differences up to44percentage points remain.
- Independent review found fixed-trample summary displayed0%; fixed through a
  failing behavior test then green. This only corrects the existing mechanic
  callout; no controls, layout or product-copy changes were added.
- Ruling: retain Shield Wall/Gothikon as explicit blockers rather than invent
  unsourced mechanics. Cost: integration remains partial until their rules are
  resolved. No inference from matching winners substitutes for full calibration.

- Initial 15 frame snapshots checked: counts, HP and first-N golden coordinate sets match manifests. Read-only source helper remains under ignored `calibration/lab/patch185872/`.
- Authoritative setup documentation fetched at `2cced342`; no blanket merge of the video branch (unrelated media/history).
- Setup implementation complete; all15 selected inputs match. Candidate mechanics generated for1030 reference entries, but conditional ability integration remains partial.
- Fifteen preliminary comparisons completed,12 matching winners/3 flips. One torch-filter runtime error corrected; only that failed job rerun. Fourteen completed outputs retained. No outcome tuning.
- Recorded video time normalized to game seconds using archive clock1.7. Results and limitations: `docs/patches/185872-simulator-comparison.md`.
- Independent scoped review performed. Full runtime integration and remaining archive runs remain incomplete for the source/ability reasons in the report; no rankings or publication work started.
