# Bulky per-frame sprite dumps — regenerate with build_unit_assets.py
*/idle/
*/attack/
*/death/

# Finalized image sets are NOT committed (1+ GB): the Railway staging bucket
# (aoe2-assets) is the backup — see aoe2x/assets/publish_extras.py.
# Only the pipeline scripts + README live in git.
*.png
*.gif
*.webp
